; Angular 21 / Storybook 10 peer mismatch (legacy @angular/platform-browser-dynamic).
legacy-peer-deps=true

; ---- PRIVATE REGISTRY (placeholder — replace before publishing) ----
; Point the @rp scope at your private registry and supply a token via env.
@rp:registry=https://REPLACE-ME.private-registry.example/
//REPLACE-ME.private-registry.example/:_authToken=${NPM_TOKEN}
